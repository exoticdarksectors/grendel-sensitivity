"""Detector geometry and reconstruction.

``grendel_geometry`` and ``reco_common`` are the detector's own definitions
(see PROVENANCE.md); importing ``grendel_geometry`` builds the fiducial mesh.
``raycast`` casts particle directions through that mesh and caches the result.
"""
