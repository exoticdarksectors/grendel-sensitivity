"""Figures: GRENDEL's sensitivity contours over the published landscape.

    python -m curves.plot paper --grendel-dir RESULTS --out FIGURES

reads the scan outputs under ``RESULTS/<model>/`` (see ``results``) and the
reference curves under ``curves/data``, and writes the paper figures.
"""
